from django.db import models

# from apps.apiForRent.models import Realty
# from apps.user.models import CustomUser


# class FavoriteProperty(models.Model):
#     user = models.ForeignKey(CustomUser, on_delete=models.CASCADE, related_name='favorites')
    # property = models.ForeignKey(Realty, on_delete=models.CASCADE)
    # property = models.ForeignKey(Realty, on_delete=models.CASCADE)



# class ViewedProperty(models.Model):
#     user = models.ForeignKey(CustomUser, on_delete=models.CASCADE, related_name='viewed_properties')
#     property = models.ForeignKey(Realty, on_delete=models.CASCADE)
#
# # претендент
# class ApplicantProperty(models.Model):
#     user = models.ForeignKey(CustomUser, on_delete=models.CASCADE, related_name='viewed_properties')
#     property = models.ForeignKey(Realty, on_delete=models.CASCADE)
#


